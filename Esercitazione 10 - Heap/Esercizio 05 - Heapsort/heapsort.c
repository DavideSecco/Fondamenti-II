#include "minheap_int.h"void HeapsortMinHeap(Heap *h) {	size_t origin_size = h->size;	while (h->size >= 2) {		// Inverto il primo elemento(il più piccolo) con quello in fondo		ElemSwap(GetNodeValueHeap(h, 0), GetNodeValueHeap(h, h->size - 1));		// Ora in ultima posizione c'è l'elemento più piccolo, riduco h->size così non lo considero più		h->size--;		// In prima posizione ora c'è un elemento "grande", la movedown farà tornare sopra l'elemento più piccolo tra quelli rimasti		MoveDownMinHeap(h, 0);	}	h->size = origin_size;}